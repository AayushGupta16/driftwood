# Overrides

- Workbench macrostructure repeats the latest dashboard build because the user explicitly requested Cardinal's campaign-builder workbench shape. Forcing a marketing macrostructure would contradict the product task.
- Corporate motion personality overrides minimal's Premium default so step insertion, selection, and inspector transitions feel crisp rather than ornamental.
- Existing pure-white ground is preserved because `design/design-language.md` is the product's source of truth and the user asked for a Driftwood implementation, not a new brand system.
