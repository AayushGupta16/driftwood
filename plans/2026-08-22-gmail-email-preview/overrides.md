# Overrides

- Arial inside `.email-preview-body` only — the user asked for Gmail-client fidelity, and this is an embedded artifact rather than Driftwood chrome. The surrounding page remains Public Sans.
